TEMPLATE_SELECT_ALL = "select * from %s";
// Шаблон select запроса в классе, где реализуется взаимодействие с БД

boolean emtyOrNullList = list.isEmpty() || list == null;
// Прежде чем работать со списком выполняется проверка. Далее в методе переменная используется несколько раз в разных ситуациях

// TBD
// С поиском иных примеров возникли сложности, так как в рабочем проекте практически нет манипуляций с числами. Есть буквально одно место, где идет деление и где в качестве делителя может быть 0, но там уже все перекрыто логикой. Есть одно место, где идет деление числа с плавающей запятой, но эти данные настолько неважны, что деление выполняется максимально неправильным путем.
// Остальные рекомендации выполняются по умолчанию. Не считая локализации и преобразования строк, так как у нас этого нет.
