---------6.1---------
name - city/cityName
// Класс City, описывает город и адрес покупателя в интернет-магазине
// Более наглядное название переменной, показывающее, что речь идет именно о названии города

logic - gameLogic
// Небольшая игра "Уголки", наподобие шашек. Идет частое обращение к переменной logic, однако непонятно из названия к чему относится данная переменная. Поэтому более логичным кажется добавить, что это логика всей игры.

BOARD_DIMENTION
BOARD_SIZE
// Размеры игрововго поля в классе Board. (Константы)

team - playersTeam
// Ссылка на команду(клсс Team) в классе PLayer

SIZE - FIGURE_SIZE
// Размер игровой фигуры в классе Figure


---------6.2---------
delta
// Изменение состояние сущности на момент запроса

converter
// Конвертеры разного рода, преобразующие один формат данных в другой

controller
// В основном RestController'ы

host:port
// Если приложение конфигурируется по-разному в зависимости от стенда, используем эту пару, чтобы определить динамически меняющиеся настройки.

basketFacade
// Использовался паттерн "Фасад" для корзины интернет-магазина


---------6.3---------
root: JsonNode
indexesNode: JsonNode
// Переменная root в методе, отвечающем за десериализацию, обозначает корень всего Json сообщения. Далее он парсился и в итоге получалас indexesNode - нода, вмещающая в себе только индексы. Далее на основе этой ноды с индексами строилось хранилище индексов

listOfIndexes
listOfRequests
indexesCounter
lenghtOfURL
// Метод, который на вход получает список индексов и строит на их основе список URL-запросов, учитывая, что в одном запросе не может быть более 100 индексов и длина запроса не может превышать 2048 символов

lastFoSessionId
localDealGeneration
// Класс, который вычитывает сообщения из Kafka (порядок сообщений не соблюдается) и записывает самое новое поколение и ID сессии сущностей, чтобы передать их дальше.


---------6.4---------
sbiAuctionOrdersCount
catAuctionOrdersCount
lastSbiAuctionOrderNo
lastCatAuctionOrderNo
localDealGeneration
// Не придумал как исправить эти названия. Часть - имя сущности, остальное - функционал переменной. Если это как-то сокращать\упразднять, то понизится читабельность и повысится и без того высока логическая нагрузка.