/**
* Не могу сказать, что улучшил какие-либо из этих констант, так как они сразу писались в таком 
* виде в рабочем проекте. Далее краткое их описание и то, как константы используются в проектах, чем помогают
/*


BODY_ENCODING
BODY_TYPE
MESSAGE_TYPE
BROKER_URL
TOPIC_NAME
// Константы выше взяты из адаптеров, перекладывающих сообщения в Кафку. Значения переменных задаются из файла с настройками и далее используются по всему адаптеру. Помогает придерживаться единого стандарта, избежать ошибок, опечаток и т.д.

START_TIME
END_TIME
// Время начала и окончания работы адаптера. Задается один раз, используется везде

MAX_URL_LENGHT
// По ТЗ длина URL-запроса не должна превышать 2048 символов. Используется при пострении запроса, помогает избежать ошибок в длине

TEMPLATE_SELECT_ALL = "select * from %s";
// Шаблон запроса в адаптере, работающим с базой данных. Экономит разработчику некоторое время и нервы

GAUGE_INIT_RECORD_UPDATE
// Из проекта, где метрики собираются и выводятся в Grfana. Одно из названий счетчика. Названия хранятся в отдельном абстрактном классе и вызываются, когда нужно обратиться к конкретному счетчику или таймеру

URI_ERROR
// Ссылка на процесс, который обрабатывает ошибочные URI

EXEC_METHOD
// Исполняемый метод, передаваемый в builder